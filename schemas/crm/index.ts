import lead from './lead'
import client from './client'
import deal from './deal'
import activity from './activity'
import crmSettings from './crmSettings'

export const crmSchemas = [
  lead,
  client,
  deal,
  activity,
  crmSettings,
]

export { lead, client, deal, activity, crmSettings }
